import { Redirect, Stack } from 'expo-router'
import { useAuth } from '@clerk/clerk-expo'

export default function AuthLayout() {
  let test = false;

 // if (test) return <Redirect href="/(tabs)" />

  return <Stack />
}
